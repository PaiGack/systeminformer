这个项目是一个 System Informer is a powerful free and open source process viewer.


现在 view -> Tray icons 里面只能显示单个的 tray icon

也就是，像每个 Cpu usage、cpu history，i/o history 等每个都是一个 系统托盘的 icon，期望能有一个合并这些曲线为一个 tray icon 的功能，只显示一个 tray icon，支持选择指定要显示的 曲线（也就是将原来的多个合并为一个）

要求：
1. 不影响原有功能，只是增加一个  tray union history 的功能
2. 支持选择指定要显示的指标（可勾选，取消勾选）

union 图标里 CPU 指标保留 kernel/user 双线
指标全不选不启用
